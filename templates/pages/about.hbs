<!DOCTYPE html>
<html lang="en">
<head>
{{> head}}
</head>
<body>
    {{> header}} 
  
    <div id="page-content" class="container">
    <div class="section-w-image">
        <div class="row">
            <div class="col-sm-6 about-me-img" style="background-image: url({{body.background}});">
            </div>
            <div class="col-sm-6 col-sm-offset-6 about-description">
                <h4>ABOUT US</h4>
                {{{body.content}}}

                <h4 class="space-top">SKILLS</h4>
                {{#body.skills}}
                <p><strong>{{title}}</strong> <span class="pull-right">{{percentage}}%</span></p>
                <div class="progress progress-label">
                    <div class="progress-bar progress-bar-default animated {{class}} fadeInLeft" role="progressbar" aria-valuenow="{{percentage}}" aria-valuemin="0" aria-valuemax="100" style="width: {{percentage}}%">
                    </div>
                </div>
                {{/body.skills}}
            </div>
        </div>
    </div>

    {{> footer}}
    {{> footerScripts}}
</body>
</html>